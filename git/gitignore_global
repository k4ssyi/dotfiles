# Global gitignore
# Based on: https://github.com/github/gitignore/tree/main/Global
#
# OS artifacts, editor/IDE files, runtime caches, secrets, AI tools
# プロジェクト固有の除外は各リポジトリの .gitignore で管理する

# === OS artifacts ===

# macOS (Global/macOS.gitignore)
.DS_Store
__MACOSX/
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Windows (Global/Windows.gitignore)
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# Linux (Global/Linux.gitignore)
.fuse_hidden*
.directory
.Trash-*
.nfs*
nohup.out

# === Editor / IDE ===

# Vim / Neovim (Global/Vim.gitignore)
# Swap
[._]*.s[a-v][a-z]
!*.svg
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]
# Session
Session.vim
Sessionx.vim
# Temporary / persistent undo
.netrwhist
*~

# Tags (Global/Tags.gitignore)
TAGS
.TAGS
!TAGS/
tags
.tags
!tags/
gtags.files
GTAGS
GRTAGS
GPATH
GSYMS
cscope.files
cscope.out
cscope.in.out
cscope.po.out

# JetBrains (Global/JetBrains.gitignore simplified)
.idea/

# Cursor (Global/Cursor.gitignore)
.cursor/
.cursorignore
.cursorindexingignore

# VSCode (Global/VisualStudioCode.gitignore partial)
*.vsix

# Backup / Patch (Global/Backup.gitignore + Global/Patch.gitignore)
*.bak
*.tmp
*.orig
*.rej

# === Runtime / tools ===

# direnv
.envrc

# mise / asdf (Global/mise.gitignore + バージョン管理は個人環境で統一)
.tool-versions
mise.toml
.mise.toml
.mise.local.toml
.mise.*.local.toml
mise.local.toml
mise.*.local.toml
.mise/*.local.toml
mise/*.local.toml

# Node.js / pnpm
node_modules/
.pnpm-debug.log

# Ruby
.ruby-lsp/

# Python
.venv
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Go
coverage.out

# Docker (個人のオーバーライド)
docker-compose.override.yml

# Build local overrides
LocalMakeFile

# === Secrets (safety net) ===

.env
.env.*
!.env.example
!.env*.example
secring.*

# === AI tools ===

.aider*
**/.claude/settings.local.json
CLAUDE.local.md
**/.mcp.json
